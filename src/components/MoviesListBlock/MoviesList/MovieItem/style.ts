import styled from 'styled-components';

export const MovieItemCard = styled.li`
  color: black;
  position: relative;

  img {
    width: calc(100vw / 7);
    object-fit: cover;
    border-radius: 5px;
  }
`;
