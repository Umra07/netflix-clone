import styled from 'styled-components';

export const FeaturesListWrapper = styled.ul`
  display: flex;
  justify-content: center;
  gap: 10px;
  padding-inline: 4%;
  margin-top: -120px;
  z-index: 2;
`;
